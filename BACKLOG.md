# Backlog

Parked decisions. Not a schedule.

## research_agent: Assign a task deferred (v1)

`poll()` is undirected discovery (arXiv + GitHub topics); it cannot answer
directed synthesis questions like "what do other tools do for X." A future
version could add a summarize_research-style memo writer function for
directed questions, separate from the poll pipeline. Not scheduled — parking
here so the reasoning isn't lost.

The Roster card keeps Assign disabled: "No task queue yet — this agent
runs autonomously."
